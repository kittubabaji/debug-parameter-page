id: debug-parameter-page

info:
  name: Debug Page Exposure
  author: Curious_Hunter(SurendraWadiwa)
  severity: medium
  description: Detects pages exposing debug information via query parameters.
  tags: debug,leak,misconfig

http:
  - method: GET
    path:
      - "{{BaseURL}}/?debug=true"

    matchers:
      - type: word
        words:
          - "stack trace"
          - "debug mode"
        condition: or